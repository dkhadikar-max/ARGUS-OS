# Project Status — v5.0 Compiler Architecture / Gate Progression

Last updated: 2026-08-01 (Gate 3 Increment 1.9)

| Gate | Status | Notes |
|---|---|---|
| Gate 1 — Architecture Freeze | ✅ Complete | 7 engine files frozen since `6f73804`; verified untouched via `git diff --stat` after every subsequent increment except one explicitly authorized, additive change (Gate 3 Increment 1.5's `decision-engine.ts` edit — see below). |
| Gate 2 — Replay | ⏸️ Incomplete (external blocker) | 10/51 fixtures valid; 41/51 blocked on an Anthropic account credit exhaustion (confirmed via the real API error, not an architecture defect). See `GATE2_REPLAY_REPORT.md`. Resume tooling (`eval/run-replay-resume.ts`) is built and ready — re-run is a $0 cost check away once credits are confirmed active for the right org. |
| Gate 3 Increment 1 — Shadow Runner | ✅ Complete | `evaluate()` runs in shadow on a sampled % of real traffic, persists to `ShadowDecision`, fire-and-forget, fault-isolated. `SHADOW_MODE_ENABLED=false` by default — inert. See commit `8b9f2b7`. |
| Gate 3 Increment 1.5 — Shadow Runner safety hardening | ✅ Complete | Concurrency cap (`SHADOW_MAX_CONCURRENT`, default 2, per-process, drops excess samples rather than queuing — `agents/shadow-concurrency.ts`) + independent wall-clock timeout (`SHADOW_TIMEOUT_MS`, default 180000ms — `agents/shadow-timeout.ts`) + an independent `CircuitBreakerProvider`/`ClaudeProvider` instance for all 5 real LLM calls a shadow run makes (`shadow-runner.service.ts`'s `shadowLlmProvider`), fully isolated from `orchestrator.ts`'s live-path breaker singleton. The last piece required an additive, backward-compatible change to `decision-engine.ts`'s `evaluate()` signature (a new optional trailing `options: {synthesizer?, stageExecutor?}`) — explicitly authorized this session as the one deviation from Gate 1's zero-frozen-file-changes streak, with its own dedicated regression test (`decision-engine.test.ts`) proving the override point is actually used, not just present in source. |
| Gate 3 Increment 1.6 — Shadow circuit breaker state metrics | ✅ Complete | `CircuitBreakerProvider` gained an optional `onStateChange` hook (no-op by default, zero effect on the live path); Shadow Runner wires it to `shadow.circuit_breaker.state_change` tagged by `{state}`. Breaker-open rejections classified as `reason: "breaker_open"` on the existing `shadow.decision.error` metric. Zero frozen-file changes (not one of the 7). |
| Gate 3 Increment 1.7 — Shadow Health Card | ✅ Complete | New `GET /api/v1/admin/shadow-health` + dashboard card: status, sampling %, last decision (relative time), 24h agreement, errors (1h, via a new in-memory ring buffer — `agents/shadow-error-log.ts`, deliberately not persisted to the DB), circuit breaker state. Documented per-instance limitation surfaced directly on the card. Commit `0e1c2ff`. |
| Gate 3 Increment 2 — Dashboard + operational metrics | ✅ Complete | Decision Explorer (Shadow Decisions list + tabbed detail page: Overview/Evidence/Agent Outputs/Execution Trace) and a Shadow Mode analytics dashboard (KPI cards, volume-by-day and disagreement-breakdown charts, date-range selector) — commits `3df3229`, `c951cb6`. |
| Gate 3 Increment 1.8 — Shadow Rollout Controller | ✅ Complete | Replaces the env-var-only `SHADOW_SAMPLE_RATE_PERCENT` (removed) with DB-backed, runtime-editable rollout config: global % + per-team percent overrides (0/100 double as deny/allow-list, unified rather than a separate mechanism), optional `reason`/`expiresAt` per override (expired overrides fall back to global, not auto-deleted — no cron exists), a second soft kill switch (`ShadowRolloutConfig.enabled`) layered on top of the unchanged hard `SHADOW_MODE_ENABLED` env switch, a paginated rollout audit log (reuses the existing generic `AuditLog`, new read endpoint), and a Dry Run Preview tool built on the exact resolution function (`shadow-rollout.service.ts`'s `previewShadowSampling`) the live path uses. New `ShadowRolloutConfig`/`ShadowRolloutTeamOverride` tables, additive migration. New dashboard page `/admin/shadow-rollout`. Zero frozen-file changes. |
| Gate 3 Increment 1.9 — Shadow Health Dashboard (Live Metrics) | ✅ Complete | New `GET /api/v1/admin/shadow-live-metrics` (global only, no teamId) + dashboard page `/admin/shadow-health`, polled every 15s while the tab is open (paused when hidden): current sample rate, shadow mode ON/OFF, current concurrency vs. in-flight runs, circuit breaker state, timeout/drop counts (last hour, via a new `agents/shadow-drop-log.ts` ring buffer mirroring `shadow-error-log.ts`, plus a new reason-filtered `countShadowErrorsByReasonSince`), error rate (last hour, excludes drops from the denominator since a drop never reached `evaluate()`), and P95 latency (last hour, reuses the existing in-process `percentile()` helper over `ShadowDecision.processingTimeMs`). "Queue depth" is honestly `hasQueue: false` with explanatory copy, not a fabricated number — Increment 1.5 deliberately drops excess sampled runs rather than queuing. This endpoint deliberately skips `recordAudit` (unlike every other admin GET) since it's designed to be polled and carries no cross-tenant customer content. Zero frozen-file changes, no schema/migration. |
| Gate 4 — Production Cutover | ⛔ Blocked | Requires Gate 2 complete + Gate 3 validated under real sampled traffic. Gate 3's infrastructure, safety hardening, observability, and rollout controls are all now in place; a cautious rollout above 0% sampling (via the new Rollout Controller, not a redeploy) is the next real step once Gate 2 completes. Not authorized, not close. |

## Why this file exists

Kept as a single, current source of truth for gate status — matches the
existing convention of `GATE2_REPLAY_AUTHORIZATION.md`/`GATE2_REPLAY_REPORT.md`
for Gate 2 specifically, but tracks the whole progression in one place so
it doesn't need to be reconstructed from commit history each time.
